## 01 머신러닝 문제 접근 방법 
- 목표가 있다면 시스템을 구축하기 전에 먼저 성공을 어떻게 정의하고 측정할지, 최종 솔루션이 비즈니스와 연구의 목표에 어떤 영향을 줄지 생각해봐야 함
- 비즈니스 척도를 직접 이용해 알고리즘 성능을 재는 것이 제일 좋고, 그것이 어렵다면 간단하게 "완벽한 모델이란 무엇인가?"라는 질문의 답을 찾으면 됨
- 모델 구축은 순환 사이클의 한 부분일 뿐
### 1.1 의사 결정 참여 
- 의사 결정에 사람이 개입해야 할지도 고려해야 함
- 즉각 응답하지 않아도 될 때는 불확실한 결정을 사람이 확인하게 할 수 있음
## 02 프로토타입에서 제품까지
- 실제로는 분석팀에서 찾은 솔루션을 고성능 언어를 사용하여 더 큰 프레임워크 안에서 재구현하는 것이 일반적
- 제품 시스템에는 일회용 분석 스크립트와는 다른 요구사항이 있음 (e.g. 신뢰성, 예측 가능성, 실행환경, 메모리 요구사항 등)
## 03 제품 시스템 테스트
- **오프라인 평가**(offline evaluation): 사전에 수집한 테스트 세트를 기초로 하여 알고리즘이 만든 예측을 평가하는 방법
- **온라인 테스트**(online test) 혹은 **라이브 테스트**(live test): 전체 시스템에 알고리즘이 적용된 이후에 평가
- **A/B 테스트**: 실전에서 알고리즘을 평가해볼 수 있고 사용자들에게 모델이 노출됐을 때 예상치 못한 결과를 발견할 수 있음 (보통 A가 새모델, B가 기존 시스템)
## 04 나만의 추정기 만들기 
```python
# 변환기 만들기
# 회귀 모델: ClassifierMixin이나 RegressorMixin을 상속하고, transform 대신 predict 구현
from sklearn.base import BaseEstimator, TransformerMixin

class MyTransformer(BaseEstimator, TransformerMixin):
    def __init__(self, first_parameter=1, second_parameter=2):
        self.first_parameter = 1
        self.second_parameter = 2

    def fit(self, X, y=None):
        print("모델 학습을 시작합니다.")
        return self

    def transform(self, X):
        X_transformed = X + 1
        return X_transformed
```
## 05 더 배울 것들
### 5.1 이론
- 많은 모델이 확률, 선형 대수, 최적화 등의 이론을 사용
### 5.2 다른 머신러닝 프레임워크와 패키지 
- `statsmodels`: 좀 더 확률적 입장에서 인터페이스를 구축한 여러 선형 모델을 제공
- R: 통계 분석을 위해 특별히 설계된 언어이고 훌륭한 시각화 기능과 많은 통계 모델 패키지 활용 가능
- `vowpal wabbit`: 대량의 데이터셋과 스트리밍 데이터에 유용
- `MLlib`: 클러스터에 머신러닝 알고리즘을 분산해서 실행하는 기능
### 5.3 랭킹, 추천 시스템과 그 외 다른 알고리즘 
- 랭킹 시스템: 어떤 질문의 대답을 관련도 순으로 추출하는 랭킹, 검색 엔진이 동작하는 원리
- 추천 시스템: 사용자의 기호에 맞게 제안을 추천하는 시스템
- 시계열 예측 ...
### 5.4 확률적 모델링, 추론, 확률적 프로그래밍
- 종종 특정한 구조를 가진 문제는 확률 이론을 사용하여 표현 가능 👉 예측하려는 환경을 수학적으로 모델링하여 만들어냄
### 5.5 신경망 
### 5.6 대규모 데이터셋으로 확장 
- **외부 메모리 학습**(out-of-core): 메모리에 저장할 수 없는 데이터로 학습하는 것, 학습이 하나의 컴퓨터에서 수행됨
- **클러스터 병렬화**(parallelization over a cluster): 클러스터를 구성하는 여러 컴퓨터로 데이터를 분산해서 각 컴퓨터가 해당하는 데이터를 처리하는 것
 
